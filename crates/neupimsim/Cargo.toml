[package]
name = "neupimsim"
version = "0.1.0"
edition = "2021"

[dependencies]
neupimrust.workspace = true
tracing.workspace = true
serde_json = { workspace = true }
clap = {workspace = true, features = ["derive"]}
